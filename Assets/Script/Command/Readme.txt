这里使用的是命令模式
命令模式是通过记录一个命令所需要调用的接口来实现调用的切换。
只要切换掉接口，那么命令就有了不同的结果。

我的实现是每一个接口不保存任何状态，这样可以方便的迁移。
同时接口控制（调用）的是固定的组件（直接调用传入组件的方法），所以只要一个对象中有对应的组件，就能够灵活的使用命令模式进行控制。

现在的命令模式主要是用来处理按键。

一个是通过鼠标控制镜头， 一个是使用键盘控制移动。